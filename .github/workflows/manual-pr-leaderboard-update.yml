name: Manually Update PR Leaderboard

on:
  workflow_dispatch:  # This enables the action to be triggered manually via a button in the GitHub UI.

jobs:
  update_leaderboard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas matplotlib  # Add other dependencies if required

      - name: Run PR Leaderboard Script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 src/scripts/generate_leaderboard_script.py  # Adjust the path as necessary

      - name: Commit and push updated leaderboard
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions"
          git add ranking_output.md
          git commit -m "Update PR leaderboard"
          git pull --rebase origin main
          git push origin HEAD:main || echo "No changes to commit"
